// @ts-check
import { defineConfig } from 'astro/config';

// Static marketing site — no backend. Decap CMS commits to GitHub, Netlify rebuilds.
export default defineConfig({
  site: 'https://www.communityassistbali.com',
});
